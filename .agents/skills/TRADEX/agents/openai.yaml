interface:
  display_name: "TRADEX"
  short_description: "Repo-specific patterns and workflows for TRADEX"
  default_prompt: "Use the TRADEX repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true